import { paths } from "../common";

export class ExperienceService {
    private readonly basePath = paths.BASE_URL + paths.EXPERIENCES;

    //TODO
    // POST /
    // GET /category/{category}
    // GET /name/{name}
    // GET, PUT y DELETE /experience/{experienceId}
    // GET y PUT /experience/{experienceId}/experienceImage
    // GET y POST /experience/{experienceId}/reviews
    // PUT /experience/{experienceId}/fav
    // PUT /experience/{experienceId}/observable

}